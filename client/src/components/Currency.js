// Used to diplay the currency and current exchange rate
// rafce

const Currency = ({name, value}) => {

    return (
        <div>
            {name}: ${value}
        </div>
    )
}

export default Currency

